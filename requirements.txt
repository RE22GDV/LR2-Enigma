# Ядро шифру та атаки працюють на чистій стандартній бібліотеці Python 3.10+.
# Наведені пакети потрібні лише для тестів, графіків експериментів і складання звіту.
pytest>=8.0
matplotlib>=3.8
numpy>=1.26
reportlab>=4.0
python-docx>=1.1
